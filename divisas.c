#include <stdio.h>
#include <math.h>
/*
Autor: Cecilia L.
Description: covertidor de pesos a (Libras, Dolares Americanos, Euros)
*/
int main()¨{
//Declarar e inicializar 2 variables o mas (l, d,  e)
float p = 1, d = 20, resultadoMultiplicacion = 20
int operacion, opcion;
printf("-----------------------------");
printf("que quieres hacer?");
printf("1.- Realizar operaciones\n 2.- Salir ");
if 1.- printf("A que moneda quieres cambiar los pesos?: ");
printf("1.-Dolares \n 2.- Euros \n 3.- Libras \n 4.- Salir\n ")
scanf("%d", &opcion)
int d = 20 ;
int e = 24 ;
int l = 28 ;
int p = 1 ;
int operacion, guardar suma;
//ingresar los datos (p, l, d, e)
scanf ("% d", &p);
scanf("%d" , &l)
scanf("%d" , &d)
scanf("%d" , &e)
//elegir el tipo de moneda (l, d ,e)
printf("A que moneda quieres cambiar los pesos?: ");
printf("1.-Dolares \n 2.- Euros \n 3.- Libras \n 4.- Salir\n ")
// ingresa el monto
printf("ingresa el monto: ");
res= p * l 
res= p * d
res= p * e
scanf("%d", operacion)
//imprimir el resultado
    switch (operacion){
        //declarations
        // . . . 
    case 1 :
        guardar operacion = p * d
        printf ("\tEl resultado de %d * %d = %d\n", d, e, l, p * d);
        break;

    case 2 :
        guardar operacion = p * e
         printf ("\tEl resultado de %d * %d = %d\n", d, e, l, p * e);
        break;

    case 3:
        guardar operacion = p * l
         printf ("\tEl resultado de %d * %d = %d\n", d, e, l, p * l);
        break;
    
    default:
        printf("salir\n");
        break;
    }
    
